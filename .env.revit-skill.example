# Copy to a private file (NOT committed), e.g. ~/.config/revit-skill.env
# Then load it before running commands from docs:
# source ~/.config/revit-skill.env

export REVIT_WINDOWS_HOST=172.26.0.136
export GITLAB_HOST=192.168.100.165
export DEV_HOST=192.168.100.120
export UNRAID_BR1_SRC=192.168.100.166

# Optional secrets (private only, do NOT commit)
# export WINDOWS_USER=Administrator
# export WINDOWS_PASSWORD='your-password'
# export GITLAB_TOKEN='glpat-...'
