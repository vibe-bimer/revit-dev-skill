image: alpine:3.20

stages:
  - guard

guard:sensitive-scan:
  stage: guard
  before_script:
    - apk add --no-cache bash ripgrep
  script:
    - bash scripts/check-revit-skill-guard.sh .
  rules:
    - if: '$CI_PIPELINE_SOURCE == "push" || $CI_PIPELINE_SOURCE == "merge_request_event"'
